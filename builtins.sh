
# ':' ( colon ) is a builtin that relates to truth value and is faster
# This can be used in situations where we only need to set defaults or
# parameter substitution and are not interested in the output
# parameters/defaults are passed as argument to ':'
# exit status is set to 0

: {PLATFORM:=REDHAT7}
